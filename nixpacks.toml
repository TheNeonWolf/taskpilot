[providers]
providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r ml/requirements.txt"]

[start]
cmd = "cd ml && /opt/venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"